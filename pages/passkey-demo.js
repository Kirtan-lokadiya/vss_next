export default function PasskeyDemoDisabled() {
  return (
    <div style={{minHeight:'60vh',display:'flex',alignItems:'center',justifyContent:'center',padding:'2rem'}}>
      <div style={{maxWidth:640}}>
        <h1 style={{fontSize:22,marginBottom:8}}>Passkey Demo Disabled</h1>
        <p>This demo route was disabled to keep only production flows. Use the normal app pages to set password, fetch passkeys, and decrypt notes.</p>
      </div>
    </div>
  );
} 